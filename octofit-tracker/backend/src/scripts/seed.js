import { execSync } from 'node:child_process';

console.log('Seed the octofit_db database with test data');
console.log('Run: npx tsx src/scripts/seed.ts');
console.log('To seed the database, use: npm run seed');
